extends Node
# Add an optional "apply_difficulty" template to existing enemies/coins
# so chunks can scale their difficulty when spawned

# This goes into something like slime.gd or coin.gd
# func apply_difficulty(multiplier: float):
#     # e.g., increase speed or spawn rate
#     SPEED *= multiplier
